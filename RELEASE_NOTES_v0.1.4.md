# Format Forge v0.1.4

本次更新带来了完整的图片裁剪工作流，并优化了大图、长图和 ICO 图标制作时的预览体验。

## 主要更新

- 新增普通图片裁剪：可拖动裁剪框，并通过边缘或右下角手柄调整裁剪范围。
- 新增输出分辨率设置：裁剪后可直接指定输出宽度和高度。
- 新增比例锁定：锁定后，裁剪框和输出分辨率会保持相同宽高比例。
- 新增 ICO 专属裁剪：使用独立的正方形裁剪框，可选择 `16px`、`32px`、`64px` 或 `256px` 输出尺寸。

## 体验优化

- 大图和长图会自动等比缩放至裁剪区域，不再出现预览错位。
- ICO 裁剪框支持移动和缩放，并始终限制在图片范围内。
- 裁剪窗口显示原图分辨率；普通图片的输出分辨率、比例锁定和裁剪操作整合为一行。
- 移除裁剪预览旁的黑色背景和 ICO 裁剪框外的黑色遮罩。
- 修复部分图片因 EXIF 方向信息导致的转换或裁剪方向错误。
- 更新应用图标资源。

## 下载

本版本提供以下平台的构建产物：

- macOS Apple Silicon
- Windows x64
- Windows ARM64

请在本次发布对应的 GitHub Actions 构建产物中下载适合自己设备的安装包。
